{
  "summary": "A paged product tour in a dialog, one step at a time, with a count or a progress bar.",
  "props": [
    {
      "name": "trigger",
      "type": "none",
      "typeName": "ReactNode",
      "example": "Start your journey",
      "description": "The trigger button's label."
    },
    {
      "name": "triggerIcon",
      "type": "none",
      "typeName": "ReactNode",
      "exampleIcon": "SparksSolid",
      "description": "A glyph beside the trigger's label."
    },
    {
      "name": "iconPosition",
      "type": "enum",
      "values": ["leading", "trailing"],
      "default": "leading",
      "description": "Which end of the trigger its `triggerIcon` sits at."
    },
    {
      "name": "steps",
      "type": "none",
      "typeName": "OnboardingStep[]",
      "example": [
        {
          "icon": {
            "$icon": "SparksSolid"
          },
          "title": "Welcome aboard",
          "description": "A short tour of what you can do here, three screens long."
        },
        {
          "icon": {
            "$icon": "Bookmark"
          },
          "title": "Build your first page",
          "description": "Drop in a component, adjust its props, and watch it update."
        },
        {
          "icon": {
            "$icon": "User"
          },
          "title": "Invite your team",
          "description": "Share a link and everyone edits the same draft."
        }
      ],
      "description": "Each step's `icon`, `title` and `description`. Steps advance one at a time and the dialog resets to the first when it closes."
    },
    {
      "name": "indicator",
      "type": "enum",
      "values": ["count", "progress", "dots"],
      "default": "count",
      "description": "`count` reads \"1 / 3\" in the header. `progress` draws a filling bar under the slide instead. `dots` moves navigation to a bottom row of step dots between prev/next arrows."
    },
    {
      "name": "dismissible",
      "type": "boolean",
      "default": false,
      "description": "An X in the corner, so the tour can be skipped without walking to the end."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["rounded", "square", "squircle"],
      "default": "rounded",
      "description": "Corner radius on the trigger, the popup and its controls."
    },
    {
      "name": "shadow",
      "type": "enum",
      "values": ["none", "inset", "outset"],
      "default": "none",
      "description": "Depth on the popup."
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "The slide transition between steps and the popup's scale-in. Turn it off for an instant tour, or when the user has asked for reduced motion."
    },
    {
      "name": "container",
      "type": "none",
      "typeName": "HTMLElement | null",
      "description": "Where the popup is rendered. Defaults to `document.body`, which is right almost always; pass an element to portal somewhere else, such as inside a frame or a container that owns its own stacking context."
    }
  ]
}
