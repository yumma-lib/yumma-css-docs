{
  "summary": "A confirmation dialog with a badge, title, description and a cancel/confirm pair, in two tones.",
  "props": [
    {
      "name": "trigger",
      "type": "none",
      "typeName": "ReactNode",
      "example": "Delete file",
      "description": "The trigger button's label."
    },
    {
      "name": "triggerIcon",
      "type": "none",
      "typeName": "ReactNode",
      "example": null,
      "exampleIcon": "Trash",
      "description": "A glyph beside the trigger's label."
    },
    {
      "name": "triggerIconPosition",
      "type": "enum",
      "values": ["leading", "trailing"],
      "default": "leading",
      "description": "Which end of the trigger `triggerIcon` sits at.",
      "dependsOn": "triggerIcon"
    },
    {
      "name": "triggerTone",
      "type": "enum",
      "values": ["danger", "neutral"],
      "default": "danger",
      "description": "The trigger's own color, independent of the dialog's `tone` - a neutral button can still open a destructive dialog."
    },
    {
      "name": "icon",
      "type": "none",
      "typeName": "ReactNode",
      "description": "The glyph in the badge above the title. No badge renders without one."
    },
    {
      "name": "tone",
      "type": "enum",
      "values": ["danger", "neutral"],
      "default": "danger",
      "description": "Colors the badge and the confirm button together."
    },
    {
      "name": "title",
      "type": "string",
      "example": "Delete file?",
      "description": "The dialog's heading, wired up by Base UI's own AlertDialog.Title."
    },
    {
      "name": "description",
      "type": "string",
      "example": "This deletes readme.md. It cannot be undone.",
      "description": "The body text, wired up by Base UI's own AlertDialog.Description."
    },
    {
      "name": "cancelLabel",
      "type": "string",
      "default": "Cancel",
      "description": "The dismissing button's label."
    },
    {
      "name": "confirmLabel",
      "type": "string",
      "example": "Delete",
      "description": "The confirming button's label."
    },
    {
      "name": "onConfirm",
      "type": "none",
      "typeName": "() => void",
      "description": "Called when the confirm button is pressed. The dialog closes either way."
    },
    {
      "name": "showClose",
      "type": "boolean",
      "default": true,
      "description": "The X in the corner. The Cancel button remains either way, so turning this off never traps anyone."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["rounded", "square", "squircle"],
      "default": "square",
      "description": "Corner radius on the popup, the badge and the buttons. `square` squares off the badge too."
    },
    {
      "name": "shadow",
      "type": "enum",
      "values": ["none", "inset", "outset"],
      "default": "none",
      "description": "Depth on the popup."
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "The backdrop's fade and the popup's scale-in. Turn it off for an instant dialog, or when the user has asked for reduced motion."
    },
    {
      "name": "container",
      "type": "none",
      "typeName": "HTMLElement | null",
      "description": "Where the popup is rendered. Defaults to `document.body`, which is right almost always; pass an element to portal somewhere else, such as inside a frame or a container that owns its own stacking context."
    }
  ]
}
