{
  "summary": "A hover tooltip, light or dark, positionable on any side, with an optional arrow.",
  "props": [
    {
      "name": "trigger",
      "type": "none",
      "typeName": "ReactNode",
      "exampleIcon": "BellNotification",
      "description": "The trigger's content - an icon, usually. Pair it with `triggerLabel` when there is no text."
    },
    {
      "name": "triggerLabel",
      "type": "string",
      "example": "Notifications",
      "description": "The trigger's `aria-label`. An icon-only trigger has no accessible name without it."
    },
    {
      "name": "content",
      "type": "none",
      "typeName": "ReactNode",
      "example": "3 unread notifications",
      "description": "The tooltip's text."
    },
    {
      "name": "side",
      "type": "enum",
      "values": ["top", "right", "bottom", "left"],
      "default": "top",
      "description": "Which side of the trigger the tooltip opens on. It flips automatically if there is no room."
    },
    {
      "name": "sideOffset",
      "type": "number",
      "default": 8,
      "description": "Gap between the trigger and the tooltip, in pixels."
    },
    {
      "name": "tone",
      "type": "enum",
      "values": ["light", "dark", "danger"],
      "default": "light",
      "description": "The tooltip's surface and its trigger together. `light` and `dark` are the two neutral surfaces; `danger` reads red at both ends, for a destructive action."
    },
    {
      "name": "arrow",
      "type": "boolean",
      "default": false,
      "description": "A pointer notched into the tooltip's edge, aimed back at the trigger. It re-aims itself when `side` changes."
    },
    {
      "name": "delay",
      "type": "number",
      "default": 300,
      "description": "How long a hover must rest before it opens, in ms. Set to `0` to open on contact."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["rounded", "square", "squircle"],
      "default": "rounded",
      "description": "Corner radius on the tooltip."
    },
    {
      "name": "shadow",
      "type": "enum",
      "values": ["none", "inset", "outset"],
      "default": "none",
      "description": "Depth on the tooltip."
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "The tooltip's fade and rise. Turn it off for an instant tooltip, or when the user has asked for reduced motion."
    },
    {
      "name": "container",
      "type": "none",
      "typeName": "HTMLElement | null",
      "description": "Where the popup is rendered. Defaults to `document.body`, which is right almost always; pass an element to portal somewhere else, such as inside a frame or a container that owns its own stacking context."
    }
  ]
}
