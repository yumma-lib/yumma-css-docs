{
  "summary": "A single show/hide panel with a labelled trigger, two icon styles and an optional disabled state.",
  "props": [
    {
      "name": "trigger",
      "type": "none",
      "typeName": "ReactNode",
      "example": "Implement user dashboard",
      "description": "The trigger's label."
    },
    {
      "name": "defaultOpen",
      "type": "boolean",
      "default": false,
      "description": "Starting state when you are not controlling it. Pair `open` with `onOpenChange` instead if you are."
    },
    {
      "name": "open",
      "type": "boolean",
      "description": "Controlled state."
    },
    {
      "name": "onOpenChange",
      "type": "none",
      "typeName": "(open: boolean) => void",
      "description": "Called with the new state. Required for a controlled collapsible."
    },
    {
      "name": "disabled",
      "type": "boolean",
      "default": false,
      "description": "Locks the panel open, replaces the icon with a lock & blocks toggling entirely - a disabled collapsible always shows its content, it does not hide it."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["square", "squircle"],
      "default": "square",
      "description": "Corner radius on the trigger. `square` (no radius) is the plain default here - no rounded variant exists, since none was ever shown for this component."
    },
    {
      "name": "icon",
      "type": "enum",
      "values": ["chevron", "plus-minus"],
      "default": "chevron",
      "description": "`chevron` rotates one glyph 90\u00b0. `plus-minus` swaps between two glyphs with the same rotation flourish."
    },
    {
      "name": "iconPosition",
      "type": "enum",
      "values": ["leading", "trailing"],
      "default": "trailing",
      "description": "Which end of the trigger the icon sits at.",
      "dependsOn": "icon"
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "The icon's rotation & the panel's height/opacity animation. Turn it off for an instant expand/collapse, or when the user has asked for reduced motion."
    }
  ],
  "children": "Panel content goes here."
}
