{
  "summary": "A navigation trail, plain or in a bordered card, with three sizes and two separator styles.",
  "props": [
    {
      "name": "items",
      "type": "none",
      "typeName": "BreadcrumbItem[]",
      "example": [
        {
          "label": "Home",
          "href": "#"
        },
        {
          "label": "Projects",
          "href": "#"
        },
        {
          "label": "Team",
          "href": "#"
        },
        {
          "label": "Board"
        }
      ],
      "description": "Each item's `label`, `href`, optional `icon` and `iconOnly`. The last item is always rendered as the current page - a plain span with `aria-current=\"page\"`, never a link - regardless of whether it has an `href`."
    },
    {
      "name": "bordered",
      "type": "boolean",
      "default": false,
      "description": "Wraps the trail in a bordered card."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["rounded", "square", "squircle"],
      "default": "rounded",
      "description": "Corner radius. Only visible when `bordered` is set.",
      "conflictsWith": [
        {
          "prop": "bordered",
          "set": false
        }
      ]
    },
    {
      "name": "shadow",
      "type": "enum",
      "values": ["none", "inset", "outset"],
      "default": "none",
      "description": "Depth on the card. Only visible when `bordered` is set.",
      "conflictsWith": [
        {
          "prop": "bordered",
          "set": false
        }
      ]
    },
    {
      "name": "size",
      "type": "enum",
      "values": ["sm", "md", "lg"],
      "default": "md",
      "description": "Text size of every item."
    },
    {
      "name": "separator",
      "type": "enum",
      "values": ["chevron", "slash"],
      "default": "chevron",
      "description": "The mark between items."
    }
  ]
}
