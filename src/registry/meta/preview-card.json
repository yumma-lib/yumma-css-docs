{
  "summary": "A hover-triggered card, in three shapes with an optional shadow.",
  "props": [
    {
      "name": "trigger",
      "type": "none",
      "typeName": "ReactNode",
      "example": "John",
      "description": "The inline content that opens the card on hover or focus."
    },
    {
      "name": "defaultOpen",
      "type": "boolean",
      "default": false,
      "description": "Starting state when you are not controlling it. Pair `open` with `onOpenChange` instead if you are."
    },
    {
      "name": "open",
      "type": "boolean",
      "description": "Controlled state.",
      "controlled": true
    },
    {
      "name": "onOpenChange",
      "type": "none",
      "typeName": "(open: boolean) => void",
      "description": "Called with the new state. Required for a controlled preview card."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["rounded", "square", "squircle"],
      "default": "square",
      "description": "Corner radius on the card. `squircle` uses `corner-shape`, which degrades to a rounded square where that is unsupported."
    },
    {
      "name": "shadow",
      "type": "enum",
      "values": ["none", "inset", "outset"],
      "default": "none",
      "description": "Depth on the card. `inset` reads as a well, `outset` as a raised control."
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "The card's fade in/out. Turn it off for an instant appearance, or when the user has asked for reduced motion."
    },
    {
      "name": "container",
      "type": "none",
      "typeName": "HTMLElement | null",
      "description": "Where the popup is rendered. Defaults to `document.body`, which is right almost always; pass an element to portal somewhere else, such as inside a frame or a container that owns its own stacking context."
    }
  ],
  "childrenExample": [
    {
      "component": "Avatar",
      "props": {
        "src": "https://api.dicebear.com/9.x/notionists/svg?seed=John&backgroundColor=DAF0B9",
        "name": "John",
        "size": "sm",
        "status": "online"
      }
    },
    {
      "text": "John, Frontend Developer"
    },
    {
      "text": "3 tasks assigned"
    }
  ]
}
