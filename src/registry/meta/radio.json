{
  "summary": "A single choice from a group, in three sizes, three shapes and three shadows, with an optional description and icon per option.",
  "props": [
    {
      "name": "options",
      "type": "none",
      "typeName": "RadioOption[]",
      "example": [
        {
          "value": "free",
          "label": "Free plan",
          "icon": {
            "$icon": "User",
            "size": "w-4 h-4"
          }
        },
        {
          "value": "pro",
          "label": "Pro plan",
          "icon": {
            "$icon": "Star",
            "size": "w-4 h-4"
          }
        },
        {
          "value": "max",
          "label": "Max plan",
          "icon": {
            "$icon": "StatUp",
            "size": "w-4 h-4"
          }
        }
      ],
      "description": "The choices. `value` and `label` are required; `description` and `icon` are optional per option. The shape is fixed rather than generic because you own the file: data that does not fit is an edit to the option body, not an API."
    },
    {
      "name": "label",
      "type": "string",
      "example": "Choose a plan",
      "description": "A caption above the group, correctly wired to it via `aria-labelledby`. Without one the group has no accessible name at all."
    },
    {
      "name": "defaultValue",
      "type": "string",
      "example": "free",
      "description": "The option selected when you are not controlling the group. Pair `value` with `onValueChange` instead if you are."
    },
    {
      "name": "value",
      "type": "string",
      "description": "Controlled selection."
    },
    {
      "name": "onValueChange",
      "type": "none",
      "typeName": "(value: string) => void",
      "description": "Called with the newly selected value. Required for a controlled group."
    },
    {
      "name": "size",
      "type": "enum",
      "values": ["sm", "md", "lg"],
      "default": "md",
      "description": "The circle, the dot and the label together."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["rounded", "square", "squircle"],
      "default": "square",
      "description": "Corner radius. `squircle` uses `corner-shape`, which degrades to a rounded square where that is unsupported."
    },
    {
      "name": "shadow",
      "type": "enum",
      "values": ["none", "inset", "outset"],
      "default": "none",
      "description": "Depth on an unselected circle. A selected one is already a solid fill, so it does not carry the shadow: stacking one on top muddies the color."
    },
    {
      "name": "disabled",
      "type": "boolean",
      "default": false,
      "description": "Blocks interaction and dims every option."
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "The dot's transition when selection changes. Turn it off for a static group, or when the user has asked for reduced motion."
    }
  ]
}
