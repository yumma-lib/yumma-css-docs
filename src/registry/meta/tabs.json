{
  "summary": "A tab list with a sliding indicator, horizontal or vertical, in three sizes and four shapes, with optional icons, counters and panels.",
  "props": [
    {
      "name": "items",
      "type": "none",
      "typeName": "TabItem[]",
      "example": [
        {
          "value": "general",
          "label": "General",
          "icon": {
            "$icon": "Wrench",
            "size": "w-4 h-4"
          }
        },
        {
          "value": "appearance",
          "label": "Appearance",
          "icon": {
            "$icon": "SunLight",
            "size": "w-4 h-4"
          }
        },
        {
          "value": "shortcuts",
          "label": "Shortcuts",
          "icon": {
            "$icon": "Bookmark",
            "size": "w-4 h-4"
          }
        }
      ],
      "description": "Each tab's `value` and `label`, plus optional `icon`, `iconOnly`, `count`, `disabled` and `panel`. `label` stays the accessible name when `iconOnly` hides it."
    },
    {
      "name": "defaultValue",
      "type": "string",
      "description": "The tab selected on first render. Falls back to the first item. Pair `value` with `onValueChange` instead if you are controlling it."
    },
    {
      "name": "value",
      "type": "string",
      "description": "Controlled selection."
    },
    {
      "name": "onValueChange",
      "type": "none",
      "typeName": "(value: string) => void",
      "description": "Called with the newly selected tab's `value`."
    },
    {
      "name": "orientation",
      "type": "enum",
      "values": ["horizontal", "vertical"],
      "default": "horizontal",
      "description": "`vertical` stacks the tabs and sits any panels beside them rather than below."
    },
    {
      "name": "size",
      "type": "enum",
      "values": ["sm", "md", "lg"],
      "default": "md",
      "description": "Tab padding and label size together."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["pill", "rounded", "square", "squircle"],
      "default": "square",
      "description": "Corner radius on the track and the tabs. `pill` steps down to `rounded` when `orientation` is vertical: a capsule resolves its radius against the narrow axis, so the track becomes a blob and the selected tab's indicator is clipped to the curve."
    },
    {
      "name": "iconPosition",
      "type": "enum",
      "values": ["leading", "trailing"],
      "default": "leading",
      "description": "Which end of a tab its `icon` sits at. Ignored when `iconOnly` is set."
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "The indicator's slide between tabs. Turn it off for an instant jump, or when the user has asked for reduced motion."
    }
  ]
}
