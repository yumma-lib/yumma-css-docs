{
  "summary": "A horizontal or vertical divider, plain, or with a clickable icon or a text label in the middle.",
  "props": [
    {
      "name": "orientation",
      "type": "enum",
      "values": ["horizontal", "vertical"],
      "default": "horizontal",
      "description": "A vertical rule for a row layout, like a button group. Applies to the `icon` and `label` forms too, which turn with it."
    },
    {
      "name": "icon",
      "type": "none",
      "typeName": "ReactNode",
      "description": "Renders as a clickable button in the middle, sized & shaped to match. Wins over `label` if both are set.",
      "exampleIcon": "Plus",
      "example": null
    },
    {
      "name": "onIconClick",
      "type": "none",
      "typeName": "() => void",
      "description": "Called when the icon button is pressed."
    },
    {
      "name": "label",
      "type": "none",
      "typeName": "ReactNode",
      "example": "or",
      "description": "Renders as plain text in the middle. Ignored if `icon` is set.",
      "conflictsWith": [
        {
          "prop": "icon",
          "set": true
        }
      ]
    },
    {
      "name": "iconShape",
      "type": "enum",
      "values": ["rounded", "square", "squircle", "circle"],
      "default": "rounded",
      "description": "Corner radius on the icon button, which is the only part with corners. Only visible alongside `icon`.",
      "conflictsWith": [
        {
          "prop": "icon",
          "set": false
        }
      ]
    }
  ]
}
