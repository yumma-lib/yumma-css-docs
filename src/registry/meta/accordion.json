{
  "summary": "A vertically stacked set of collapsible panels, in four variants, with single or multiple selection.",
  "props": [
    {
      "name": "items",
      "type": "none",
      "typeName": "AccordionItem[]",
      "example": [
        {
          "value": "storage",
          "title": "Where are my files kept?",
          "content": "In the folder you chose on setup. Move it any time from Settings."
        },
        {
          "value": "notifications",
          "title": "How do I change notifications?",
          "content": "Open Settings, then Notifications, and pick what raises an alert."
        },
        {
          "value": "shortcuts",
          "title": "Can I change the shortcuts?",
          "content": "Every shortcut is editable in Settings. Clear a field to drop one."
        }
      ],
      "description": "Each item's `value`, `title`, `content` and optional `disabled`. `value` is what `defaultValue`/`value` reference to control which items are open."
    },
    {
      "name": "variant",
      "type": "enum",
      "values": ["default", "bordered", "ghost", "subtle"],
      "default": "default",
      "description": "`default` is a plain list divided by `separator`. `bordered` boxes each item. `ghost` marks the open item with a left border. `subtle` fills the open item's background."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["rounded", "square", "squircle"],
      "default": "square",
      "description": "Corner radius on `bordered` items. The other variants use a fixed radius, since no shape was ever paired with them.",
      "conflictsWith": [
        {
          "prop": "variant",
          "is": "default"
        },
        {
          "prop": "variant",
          "is": "ghost"
        }
      ]
    },
    {
      "name": "shadow",
      "type": "enum",
      "values": ["none", "inset", "outset"],
      "default": "none",
      "description": "Wraps the whole list in a card. Only applies to the `default` variant.",
      "conflictsWith": [
        {
          "prop": "variant",
          "is": "ghost"
        }
      ]
    },
    {
      "name": "separated",
      "type": "boolean",
      "default": true,
      "description": "Draws a rule between each pair of items. Only visible on the `default` variant.",
      "conflictsWith": [
        {
          "prop": "variant",
          "not": "default"
        }
      ]
    },
    {
      "name": "indicator",
      "type": "enum",
      "values": ["chevron", "plus-minus"],
      "default": "chevron",
      "description": "Which built-in open/closed mark the trigger draws. `chevron` rotates one glyph 180 degrees. `plus-minus` swaps between two glyphs with a small rotation flourish."
    },
    {
      "name": "indicatorPosition",
      "type": "enum",
      "values": ["leading", "trailing"],
      "default": "trailing",
      "description": "Which end of the trigger the indicator sits at.",
      "dependsOn": "indicator",
      "conflictsWith": [
        {
          "prop": "indicator",
          "is": "chevron"
        }
      ]
    },
    {
      "name": "multiple",
      "type": "boolean",
      "default": false,
      "description": "Allow more than one item open at once. Off keeps opening one item closed the rest."
    },
    {
      "name": "defaultValue",
      "type": "none",
      "typeName": "string[]",
      "example": ["notifications"],
      "description": "Which items start open, by `value`. Pair `value` with `onValueChange` instead if you are controlling it."
    },
    {
      "name": "value",
      "type": "none",
      "typeName": "string[]",
      "description": "Controlled open items, by `value`."
    },
    {
      "name": "onValueChange",
      "type": "none",
      "typeName": "(value: string[]) => void",
      "description": "Called with the new open items. Required for a controlled accordion."
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "The icon's rotation & the panel's height/opacity animation. Turn it off for an instant expand/collapse, or when the user has asked for reduced motion."
    }
  ]
}
