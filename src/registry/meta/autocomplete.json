{
  "summary": "A text input that filters a list as you type, in three sizes, three shapes and three shadows, with an optional icon and a loading state.",
  "props": [
    {
      "name": "items",
      "type": "none",
      "typeName": "AutocompleteItem[]",
      "example": [
        {
          "label": "Berlin",
          "description": "Germany"
        },
        {
          "label": "Lisbon",
          "description": "Portugal"
        },
        {
          "label": "Nairobi",
          "description": "Kenya"
        },
        {
          "label": "Osaka",
          "description": "Japan"
        }
      ],
      "description": "What to search. `label` is matched and shown; `description` and `avatar` are optional. The shape is fixed rather than generic because you own the file: data that does not fit is an edit to the item body, not an API."
    },
    {
      "name": "label",
      "type": "string",
      "example": "City",
      "description": "Field label above the input. Leave it out and the input is described by its placeholder alone."
    },
    {
      "name": "description",
      "type": "string",
      "description": "A line under the input, for what the field expects rather than a restatement of the label."
    },
    {
      "name": "placeholder",
      "type": "string",
      "default": "Search",
      "description": "Placeholder text."
    },
    {
      "name": "size",
      "type": "enum",
      "values": ["sm", "md", "lg"],
      "default": "md",
      "description": "Height and width of the input. The popup matches it, or it reads as a different control."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["rounded", "square", "squircle"],
      "default": "square",
      "description": "Corner radius, applied to the input and the popup together. `squircle` uses `corner-shape`."
    },
    {
      "name": "shadow",
      "type": "enum",
      "values": ["none", "inset", "outset"],
      "default": "none",
      "description": "Depth on the input. `inset` reads as a well, `outset` as a raised control."
    },
    {
      "name": "icon",
      "type": "none",
      "typeName": "ReactNode",
      "example": null,
      "exampleIcon": "Search",
      "description": "Any icon. It is positioned over the input and the text is padded around it, so pass the glyph and nothing else."
    },
    {
      "name": "iconPosition",
      "type": "enum",
      "values": ["leading", "trailing"],
      "default": "leading",
      "description": "Which end `icon` sits at.",
      "dependsOn": "icon"
    },
    {
      "name": "disabled",
      "type": "boolean",
      "default": false,
      "description": "Blocks interaction and dims the field."
    },
    {
      "name": "loading",
      "type": "boolean",
      "default": false,
      "description": "Replaces the results with a loading row, for an async source. The fetching itself is yours; this is only the state."
    },
    {
      "name": "autoHighlight",
      "type": "boolean",
      "default": false,
      "description": "Highlights the first match as you type, so Enter takes it without an arrow key first."
    },
    {
      "name": "limit",
      "type": "number",
      "default": 0,
      "description": "Cap on how many matches are listed. `0` lists them all.",
      "min": 0,
      "max": 10,
      "step": 1
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "Fades the popup in and out. Turn it off for a static popup, or when the user has asked for reduced motion."
    },
    {
      "name": "emptyMessage",
      "type": "string",
      "default": "No results found.",
      "description": "Shown when nothing matches."
    },
    {
      "name": "container",
      "type": "none",
      "typeName": "HTMLElement | null",
      "description": "Where the popup is rendered. Defaults to `document.body`, which is right almost always; pass an element to portal somewhere else, such as inside a frame or a container that owns its own stacking context."
    }
  ]
}
