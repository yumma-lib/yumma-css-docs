{
  "summary": "A searchable command dialog, grouped, with optional per-item descriptions, shortcuts and icons.",
  "props": [
    {
      "name": "trigger",
      "type": "none",
      "typeName": "ReactNode",
      "example": "Commands",
      "description": "The trigger button's label. A search glyph is always drawn beside it."
    },
    {
      "name": "groups",
      "type": "none",
      "typeName": "CommandGroup[]",
      "example": [
        {
          "label": "Actions",
          "items": [
            {
              "id": "new-doc",
              "label": "New doc",
              "icon": {
                "$icon": "PagePlus",
                "size": "w-4 h-4"
              },
              "shortcut": "\u2318N"
            },
            {
              "id": "drafts",
              "label": "Drafts",
              "icon": {
                "$icon": "Page",
                "size": "w-4 h-4"
              },
              "shortcut": "\u2318D"
            },
            {
              "id": "templates",
              "label": "Templates",
              "icon": {
                "$icon": "Folder",
                "size": "w-4 h-4"
              }
            }
          ]
        },
        {
          "label": "Pages",
          "items": [
            {
              "id": "knowledge-base",
              "label": "Knowledge base",
              "icon": {
                "$icon": "PageSearch",
                "size": "w-4 h-4"
              },
              "description": "Everything the team has written down"
            },
            {
              "id": "analytics",
              "label": "Analytics",
              "icon": {
                "$icon": "StatUp",
                "size": "w-4 h-4"
              },
              "description": "Views, referrers and search terms"
            },
            {
              "id": "settings",
              "label": "Settings",
              "icon": {
                "$icon": "Wrench",
                "size": "w-4 h-4"
              },
              "description": "Workspace, members and billing"
            }
          ]
        }
      ],
      "description": "Each group's `label` and its `items`. An item takes `id`, `label`, and optional `description`, `shortcut`, `icon` and `onSelect`. A divider is drawn between groups automatically."
    },
    {
      "name": "placeholder",
      "type": "string",
      "default": "Search commands...",
      "description": "The search input's placeholder."
    },
    {
      "name": "emptyMessage",
      "type": "string",
      "default": "No commands found.",
      "description": "Shown when the query matches nothing."
    },
    {
      "name": "iconPosition",
      "type": "enum",
      "values": ["leading", "trailing"],
      "default": "leading",
      "description": "Which end of an item its `icon` sits at. A `shortcut` always trails regardless."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["rounded", "square", "squircle"],
      "default": "rounded",
      "description": "Corner radius on the trigger, the popup and its items."
    },
    {
      "name": "shadow",
      "type": "enum",
      "values": ["none", "inset", "outset"],
      "default": "none",
      "description": "Depth on the popup."
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "The backdrop's fade and the popup's scale-in. Turn it off for an instant palette, or when the user has asked for reduced motion."
    },
    {
      "name": "container",
      "type": "none",
      "typeName": "HTMLElement | null",
      "description": "Where the popup is rendered. Defaults to `document.body`, which is right almost always; pass an element to portal somewhere else, such as inside a frame or a container that owns its own stacking context."
    }
  ]
}
