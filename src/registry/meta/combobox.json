{
  "summary": "A filtering input that commits to a selection, in three sizes, three shapes and three shadows, single or multiple with removable chips.",
  "props": [
    {
      "name": "items",
      "type": "none",
      "typeName": "ComboboxItem[]",
      "example": [
        {
          "label": "Berlin",
          "description": "Germany"
        },
        {
          "label": "Lisbon",
          "description": "Portugal"
        },
        {
          "label": "Nairobi",
          "description": "Kenya"
        },
        {
          "label": "Osaka",
          "description": "Japan"
        }
      ],
      "description": "What to choose from. Same shape as Autocomplete's: `label` is matched and shown, `description` and `avatar` are optional."
    },
    {
      "name": "label",
      "type": "string",
      "example": "City",
      "description": "Field label above the input."
    },
    {
      "name": "description",
      "type": "string",
      "description": "A line under the input, for what the field expects rather than a restatement of the label."
    },
    {
      "name": "placeholder",
      "type": "string",
      "default": "Search",
      "description": "Placeholder text."
    },
    {
      "name": "size",
      "type": "enum",
      "values": ["sm", "md", "lg"],
      "default": "md",
      "description": "Height and width of the input. The popup and the trigger column follow it."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["rounded", "square", "squircle"],
      "default": "square",
      "description": "Corner radius, applied to the input and the popup together. `squircle` uses `corner-shape`."
    },
    {
      "name": "shadow",
      "type": "enum",
      "values": ["none", "inset", "outset"],
      "default": "none",
      "description": "Depth on the input. `inset` reads as a well, `outset` as a raised control."
    },
    {
      "name": "multiple",
      "type": "boolean",
      "default": false,
      "description": "Allows more than one selection. Choices become removable chips under the input."
    },
    {
      "name": "clearable",
      "type": "boolean",
      "default": true,
      "description": "Adds a button that empties the selection. It appears with the first selection rather than with the prop, because an X on an empty field has nothing to do. Works alongside `multiple`, where it clears every chip."
    },
    {
      "name": "disabled",
      "type": "boolean",
      "default": false,
      "description": "Blocks interaction and dims the field."
    },
    {
      "name": "loading",
      "type": "boolean",
      "default": false,
      "description": "Replaces the results with a loading row, for an async source. The fetching itself is yours; this is only the state."
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "Fades the popup in and out. Turn it off for a static popup, or when the user has asked for reduced motion."
    },
    {
      "name": "emptyMessage",
      "type": "string",
      "default": "No results found.",
      "description": "Shown when nothing matches."
    },
    {
      "name": "container",
      "type": "none",
      "typeName": "HTMLElement | null",
      "description": "Where the popup is rendered. Defaults to `document.body`, which is right almost always; pass an element to portal somewhere else, such as inside a frame or a container that owns its own stacking context."
    }
  ]
}
