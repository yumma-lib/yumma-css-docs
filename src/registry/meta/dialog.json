{
  "summary": "A modal dialog with a title, optional description and body, and an optional cancel/confirm pair.",
  "props": [
    {
      "name": "trigger",
      "type": "none",
      "typeName": "ReactNode",
      "example": "Rename file",
      "description": "The trigger button's label."
    },
    {
      "name": "triggerIcon",
      "type": "none",
      "typeName": "ReactNode",
      "example": null,
      "exampleIcon": "PagePlus",
      "description": "A glyph beside the trigger's label."
    },
    {
      "name": "triggerIconPosition",
      "type": "enum",
      "values": ["leading", "trailing"],
      "default": "leading",
      "description": "Which end of the trigger `triggerIcon` sits at.",
      "dependsOn": "triggerIcon"
    },
    {
      "name": "triggerTone",
      "type": "enum",
      "values": ["neutral", "danger"],
      "default": "neutral",
      "description": "Color: `neutral` or `danger`, for a destructive confirmation."
    },
    {
      "name": "triggerSize",
      "type": "enum",
      "values": ["sm", "md"],
      "default": "md",
      "description": "Footprint: `md` or `sm`, for a trigger nested inside other content, like a row action."
    },
    {
      "name": "onTriggerClick",
      "type": "none",
      "typeName": "() => void",
      "description": "Fires when the trigger is pressed, before the dialog opens, for capturing which row triggered it, say, in a dialog reused across a list."
    },
    {
      "name": "header",
      "type": "none",
      "typeName": "ReactNode",
      "description": "Content above the title: an avatar and name block, say. Centered in its own padded row."
    },
    {
      "name": "title",
      "type": "string",
      "example": "Rename file",
      "description": "The dialog's heading, wired up by Base UI's own Dialog.Title."
    },
    {
      "name": "description",
      "type": "none",
      "typeName": "ReactNode",
      "example": "Choose a new name for readme.md.",
      "description": "The body text, wired up by Base UI's own Dialog.Description."
    },
    {
      "name": "children",
      "type": "none",
      "typeName": "ReactNode",
      "description": "The body, below the description. Form fields and anything else go here. Style them yourself."
    },
    {
      "name": "cancelLabel",
      "type": "string",
      "default": "Cancel",
      "description": "The dismissing button's label."
    },
    {
      "name": "confirmLabel",
      "type": "string",
      "example": "Rename",
      "description": "The confirming button's label. No footer renders without this, so an informational dialog closes by its X alone."
    },
    {
      "name": "onConfirm",
      "type": "none",
      "typeName": "() => void",
      "description": "Called when the confirm button is pressed. The dialog closes either way."
    },
    {
      "name": "confirmTone",
      "type": "enum",
      "values": ["primary", "danger"],
      "default": "primary",
      "description": "Color of the confirm button: `primary` or `danger`, for a destructive action like a delete."
    },
    {
      "name": "showClose",
      "type": "boolean",
      "default": true,
      "description": "The X in the corner."
    },
    {
      "name": "shape",
      "type": "enum",
      "values": ["rounded", "square", "squircle"],
      "default": "square",
      "description": "Corner radius on the popup and its buttons."
    },
    {
      "name": "shadow",
      "type": "enum",
      "values": ["none", "inset", "outset"],
      "default": "none",
      "description": "Depth on the popup."
    },
    {
      "name": "animated",
      "type": "boolean",
      "default": true,
      "description": "The backdrop's fade and the popup's scale-in. Turn it off for an instant dialog, or when the user has asked for reduced motion."
    },
    {
      "name": "container",
      "type": "none",
      "typeName": "HTMLElement | null",
      "description": "Where the popup is rendered. Defaults to `document.body`, which is right almost always; pass an element to portal somewhere else, such as inside a frame or a container that owns its own stacking context."
    }
  ]
}
