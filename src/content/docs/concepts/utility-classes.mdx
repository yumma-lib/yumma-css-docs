---
title: Utility Classes
description: Use a constrained set of primitive utilities for complex components.
slug: docs/utility-classes
---

Building apps with vanilla CSS can be a lot of work, but it doesn't have to be. Yumma CSS uses an abbreviated syntax for its utility classes, which makes it easier to scale and maintain with less code, faster debugging, and more readable and understandable code.

<Aside type="danger" title="Using vanilla CSS">
  If you use regular, you’ll end up with larger files, which will take longer to split and will be harder to maintain and scale.
</Aside>

<Live data={regularCard} useHeight="360px" usePadding useCenter />

---

<Aside type="tip" title="Using Yumma CSS">
  Yumma CSS uses abbreviated syntax, which will make the files smaller. This will make it easier to maintain and scale applications without having to learn a whole new markup.
</Aside>

<Live
  data={yummaCard}
  usePadding
  useCenter
  useHeight="360px"
  useVersion="release"
/>
