---
title: Border Style
description: Controls the style of the borders of an element.
slug: docs/border-style
---

import { borderStyle } from "../../../../constants/examples/border-style";

Controls the style of the borders of an element.

<Utility
  data={[
    {
      className: "b-none",
      properties: ["border-style: none;"],
    },
    {
      className: "b-d",
      properties: ["border-style: dashed;"],
    },
    {
      className: "b-s",
      properties: ["border-style: solid;"],
    },
  ]}
/>

<Live
  data={borderStyle}
  useCenter
  useHeight="100px"
  useMarks={["b-none", "b-d", "b-h", "b-s"]}
  usePadding
  useVersion="2.1.0"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:b-*`, `md:b-*`, `lg:b-*`, and `xxl:b-*` allows targeting specific utilities in different viewports.

```html "md:b-s"
<div class="b-h md:b-s ..."></div>
```

### Using variants

Using `:hover` variants such as `h:b-*` allows you to override elements and change their values when hovering over them.

```html "h:b-s"
<div class="b-h h:b-s ..."></div>
```
