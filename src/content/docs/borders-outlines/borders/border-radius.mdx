---
title: Border Radius
description: Controls the radius of the borders of an element.
slug: docs/border-radius
---

import {
  base,
  bottomLeftRadius,
  bottomRightRadius,
  topLeftRadius,
  topRightRadius,
} from "../../../../constants/examples/borders-radius";

Controls the radius of the borders of an element.

<Utility
  additionalClasses={[
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
  ]}
  classPrefix="rad-"
  incrementValue={0.25}
  propNames={["border-radius"]}
  range={8}
  unit="rem"
/>

<Live
  additionalClasses={[
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
  ]}
  data={base}
  useCenter
  useHeight="100px"
  useMarks={["rad-0", "rad-2", "rad-4", "rad-half"]}
  usePadding
  useVersion="2.1.0"
/>

### Bottom left radius

Controls the radius of the border of the bottom left of an element.

<Utility
  additionalClasses={[
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
  ]}
  classPrefix="rad-bl-"
  incrementValue={0.25}
  propNames={["border-bottom-left-radius"]}
  range={8}
  unit="rem"
/>

<Live
  additionalClasses={[
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
  ]}
  data={bottomLeftRadius}
  useCenter
  useHeight="100px"
  useMarks={["rad-bl-2"]}
  usePadding
  useVersion="2.1.0"
/>

### Bottom right radius

Controls the radius of the border of the bottom right of an element.

<Utility
  additionalClasses={[
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
  ]}
  classPrefix="rad-br-"
  incrementValue={0.25}
  propNames={["border-bottom-right-radius"]}
  range={8}
  unit="rem"
/>

<Live
  data={bottomRightRadius}
  useCenter
  useHeight="100px"
  useMarks={["rad-br-2"]}
  usePadding
  useVersion="2.1.0"
/>

### Top left radius

Controls the radius of the border of the top left of an element.

<Utility
  additionalClasses={[
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
  ]}
  classPrefix="rad-tl-"
  incrementValue={0.25}
  propNames={["border-top-left-radius"]}
  range={8}
  unit="rem"
/>

<Live
  data={topLeftRadius}
  useCenter
  useHeight="100px"
  useMarks={["rad-tl-2"]}
  usePadding
  useVersion="2.1.0"
/>

### Top right radius

Controls the radius of the border of the top right of an element.

<Utility
  additionalClasses={[
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
  ]}
  classPrefix="rad-tr-"
  incrementValue={0.25}
  propNames={["border-top-right-radius"]}
  range={8}
  unit="rem"
/>

<Live
  data={topRightRadius}
  useCenter
  useHeight="100px"
  useMarks={["rad-tr-2"]}
  usePadding
  useVersion="2.1.0"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:rad-*`, `md:rad-*`, `lg:rad-*`, and `xxl:rad-*` allows targeting specific utilities in different viewports.

```html "md:rad-2"
<div class="rad-1 md:rad-2 ..."></div>
```

### Using variants

Using `:hover` variants such as `h:rad-*` allows you to override elements and change their values when hovering over them.

```html "h:rad-2"
<div class="rad-1 h:rad-2 ..."></div>
```
