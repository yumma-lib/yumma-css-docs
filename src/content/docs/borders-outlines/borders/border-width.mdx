---
title: Border Width
description: Controls the width of the borders of an element.
slug: docs/border-width
---

import {
  base,
  bottomWidth,
  leftWidth,
  rightWidth,
  topWidth,
} from "../../../../constants/examples/border-width";

Controls the width of the borders of an element.

<Utility
  classPrefix="b-"
  incrementValue={1}
  propNames={["border-width"]}
  range={8}
  unit="px"
/>

<Live
  data={base}
  useCenter
  useHeight="100px"
  useMarks={["b-1", "b-2", "b-3", "b-4"]}
  usePadding
  useVersion="2.1.0"
/>

### Border bottom width

Controls the width of the bottom border of an element.

<Utility
  classPrefix="bb-"
  incrementValue={1}
  propNames={["border-bottom-width"]}
  range={8}
  unit="px"
/>

<Live
  data={bottomWidth}
  useCenter
  useHeight="100px"
  useMarks={["bb-3"]}
  usePadding
  useVersion="2.1.0"
/>

### Border left width

Controls the width of the left border of an element.

<Utility
  classPrefix="bl-"
  incrementValue={1}
  propNames={["border-left-width"]}
  range={8}
  unit="px"
/>

<Live
  data={leftWidth}
  useCenter
  useHeight="100px"
  useMarks={["bl-3"]}
  usePadding
  useVersion="2.1.0"
/>

### Border right width

Controls the width of the right border of an element.

<Utility
  classPrefix="br-"
  incrementValue={1}
  propNames={["border-right-width"]}
  range={8}
  unit="px"
/>

<Live
  data={rightWidth}
  useCenter
  useHeight="100px"
  useMarks={["br-3"]}
  usePadding
  useVersion="2.1.0"
/>

### Border top width

Controls the width of the top border of an element.

<Utility
  classPrefix="bt-"
  incrementValue={1}
  propNames={["border-top-width"]}
  range={8}
  unit="px"
/>

<Live
  data={topWidth}
  useCenter
  useHeight="100px"
  useMarks={["bt-3"]}
  usePadding
  useVersion="2.1.0"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:b-*`, `md:b-*`, `lg:b-*`, and `xxl:b-*` allows targeting specific utilities in different viewports.

```html "md:b-2"
<div class="b-1 md:b-2 ..."></div>
```

### Using variants

Using `:hover` variants such as `h:b-*` allows you to override elements and change their values when hovering over them.

```html "h:b-2"
<div class="b-1 h:b-2 ..."></div>
```
