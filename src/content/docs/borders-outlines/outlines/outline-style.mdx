---
title: Outline Style
description: Controls the outline style of an element.
slug: docs/outline-style
---

import { base } from "../../../../constants/examples/outline-style";

Controls the outline style of an element.

<Utility
  data={[
    {
      className: "os-none",
      properties: ["outline-style: none;"],
    },
    {
      className: "os-d",
      properties: ["outline-style: dashed;"],
    },
    {
      className: "os-h",
      properties: ["outline-style: hidden;"],
    },
    {
      className: "os-s",
      properties: ["outline-style: solid;"],
    },
  ]}
/>

<Live
  data={base}
  useCenter
  useHeight="100px"
  useMarks={["os-none", "os-d", "os-h", "os-s"]}
  usePadding
  useVersion="release"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:os-*`, `md:os-*`, `lg:os-*`, and `xxl:os-*` allows targeting specific utilities in different viewports.

```html "md:os-s"
<div class="os-h md:os-s ..."></div>
```

### Using variants

Using `:hover` variants such as `h:os-*` allows you to override elements and change their values when hovering over them.

```html "h:os-s"
<div class="os-h h:os-s ..."></div>
```
