---
title: Opacity
description: Controls the opacity of an element.
slug: docs/opacity
---

import { base } from "../../../constants/examples/opacity";

Controls the opacity of an element.

<Utility
  classPrefix="o-"
  propNames={["opacity"]}
  incrementValue={0.1}
  round={1}
  incrementPrefix={10}
  range={10}
/>

<Live
  data={base}
  useCenter
  useHeight="100px"
  useMarks={["o-20", "o-50", "o-70", "o-100"]}
  usePadding
  useVersion="2.1.0"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:o-*`, `md:o-*`, `lg:o-*`, and `xxl:o-*` allows targeting specific utilities in different viewports.

```html "md:o-20"
<div class="o-10 md:o-20 ..."></div>
```

### Using variants

Using `:hover` variants such as `h:o-*` allows you to override elements and change their values when hovering over them.

```html "h:o-20"
<div class="o-10 h:o-20 ..."></div>
```
