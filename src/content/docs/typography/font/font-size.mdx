---
title: Font Size
description: Controls the font size of an element.
slug: docs/font-size
---

Controls the font size of an element.

<Utility
  data={[
    {
      className: "fs-xs",
      properties: ["font-size: 12px;"],
    },
    {
      className: "fs-sm",
      properties: ["font-size: 14.4px;"],
    },
    {
      className: "fs-md",
      properties: ["font-size: 16px;"],
    },
    {
      className: "fs-lg",
      properties: ["font-size: 19.2px;"],
    },
    {
      className: "fs-xl",
      properties: ["font-size: 23.04px;"],
    },
    {
      className: "fs-xxl",
      properties: ["font-size: 30.08px;"],
    },
    {
      className: "fs-3xl",
      properties: ["font-size: 35.52px;"],
    },
    {
      className: "fs-6xl",
      properties: ["font-size: 58.56px;"],
    },
  ]}
/>

<Preview
  data={`
    <p class="fs-sm">The quick brown fox jumps over the lazy dog.</p>
    <p class="fs-lg">The quick brown fox jumps over the lazy dog.</p>
    <p class="fs-xxl">The quick brown fox jumps over the lazy dog.</p>
    <p class="fs-3xl">The quick brown fox jumps over the lazy dog.</p>
    `}
  usePadding
  useVersion="release"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:fs-*`, `md:fs-*`, `lg:fs-*`, and `xxl:fs-*` allows targeting specific utilities in different viewports.

```html "md:fs-lg"
<div class="fs-md md:fs-lg ..."></div>
```

### Using variants

Using `:hover` variants such as `h:fs-*` allows you to override elements and change their values when hovering over them.

```html "h:fs-lg"
<div class="fs-md h:fs-lg ..."></div>
```
