---
title: Justify Content
description: Controls the positioning of flex and grid elements on a container's main axis.
slug: docs/justify-content
---

Controls the positioning of flex and grid elements on a container's main axis.

<Utility
  data={[
    {
      className: "jc-c",
      properties: ["justify-content: center;"],
    },
    {
      className: "jc-s",
      properties: ["justify-content: start;"],
    },
    {
      className: "jc-e",
      properties: ["justify-content: end;"],
    },
    {
      className: "jc-fs",
      properties: ["justify-content: flex-start;"],
    },
    {
      className: "jc-fe",
      properties: ["justify-content: flex-end;"],
    },
    {
      className: "jc-l",
      properties: ["justify-content: left;"],
    },
    {
      className: "jc-r",
      properties: ["justify-content: right;"],
    },
    {
      className: "jc-n",
      properties: ["justify-content: normal;"],
    },
    {
      className: "jc-sb",
      properties: ["justify-content: space-between;"],
    },
    {
      className: "jc-sa",
      properties: ["justify-content: space-around;"],
    },
    {
      className: "jc-se",
      properties: ["justify-content: space-evenly;"],
    },
    {
      className: "jc-stretch",
      properties: ["justify-content: stretch;"],
    },
  ]}
/>

## Center

<Live
  data={`<div class="d-f jc-c">
      <div class="bg-pink dim-l4 mr-3 rad-l"></div>
      <div class="bg-pink dim-l4 mr-3 rad-l"></div>
      <div class="bg-pink dim-l4 mr-3 rad-l"></div>
    </div>`}
  usePadding
  useVersion="release"
/>

## Space around

<Live
  data={`<div class="d-f jc-sa">
      <div class="bg-pink dim-l4 rad-l"></div>
      <div class="bg-pink dim-l4 rad-l"></div>
      <div class="bg-pink dim-l4 rad-l"></div>
    </div>`}
  usePadding
  useVersion="release"
/>

## Space between

<Live
  data={`<div class="d-f jc-sb">
      <div class="bg-pink dim-l4 rad-l"></div>
      <div class="bg-pink dim-l4 rad-l"></div>
      <div class="bg-pink dim-l4 rad-l"></div>
    </div>`}
  usePadding
  useVersion="release"
/>

## Space end

<Live
  data={`<div class="d-f jc-se">
      <div class="bg-pink dim-l4 rad-l"></div>
      <div class="bg-pink dim-l4 rad-l"></div>
      <div class="bg-pink dim-l4 rad-l"></div>
    </div>`}
  usePadding
  useVersion="release"
/>

## Start

<Live
  data={`<div class="d-f jc-s">
      <div class="bg-pink dim-l4 mr-3 rad-l"></div>
      <div class="bg-pink dim-l4 mr-3 rad-l"></div>
      <div class="bg-pink dim-l4 mr-3 rad-l"></div>
    </div>`}
  usePadding
  useVersion="release"
/>

## End

<Live
  data={`<div class="d-f jc-e">
      <div class="bg-pink dim-l4 mr-3 rad-l"></div>
      <div class="bg-pink dim-l4 mr-3 rad-l"></div>
      <div class="bg-pink dim-l4 mr-3 rad-l"></div>
    </div>`}
  usePadding
  useVersion="release"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:jc-*`, `md:jc-*`, `lg:jc-*`, and `xxl:jc-*` allows targeting specific utilities in different viewports.

```html "md:jc-r"
<div class="jc-l md:jc-r ..."></div>
```

### Using variants

Using `:hover` variants such as `h:jc-*` allows you to override elements and change their values when hovering over them.

```html "h:jc-r"
<div class="jc-l h:jc-r ..."></div>
```
