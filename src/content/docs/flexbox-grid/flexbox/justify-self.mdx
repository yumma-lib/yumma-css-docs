---
title: Justify Self
description: Controls the alignment of a single grid element relative to its inline axis.
slug: docs/justify-self
---

Controls the alignment of a single grid element relative to its inline axis.

<Utility
  data={[
    {
      className: "js-auto",
      properties: ["justify-self: auto;"],
    },
    {
      className: "js-n",
      properties: ["justify-self: normal;"],
    },
    {
      className: "js-stretch",
      properties: ["justify-self: stretch;"],
    },
    {
      className: "js-c",
      properties: ["justify-self: center;"],
    },
    {
      className: "js-s",
      properties: ["justify-self: start;"],
    },
    {
      className: "js-e",
      properties: ["justify-self: end;"],
    },
    {
      className: "js-fs",
      properties: ["justify-self: flex-start;"],
    },
    {
      className: "js-fe",
      properties: ["justify-self: flex-end;"],
    },
    {
      className: "js-l",
      properties: ["justify-self: left;"],
    },
    {
      className: "js-r",
      properties: ["justify-self: right;"],
    },
  ]}
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:js-*`, `md:js-*`, `lg:js-*`, and `xxl:js-*` allows targeting specific utilities in different viewports.

```html "md:js-r"
<div class="js-l md:js-r ..."></div>
```

### Using variants

Using `:hover` variants such as `h:js-*` allows you to override elements and change their values when hovering over them.

```html "h:js-r"
<div class="js-l h:js-r ..."></div>
```
