---
title: Flex Wrap
description: Controls how flex items are wrapped.
slug: docs/flex-wrap
---

Controls how flex items are wrapped.

<Utility
  data={[
    {
      className: 'fw-nw',
      properties: [
        'flex-wrap: nowrap;'
      ]
    },
    {
      className: 'fw-w',
      properties: [
        'flex-wrap: wrap;'
      ]
    },
    {
      className: 'fw-wr',
      properties: [
        'flex-wrap: wrap-reverse;'
      ]
    }
  ]}
/>

### Using breakpoints

The utilization of breakpoint modifiers, like `sm:fw-*`, `md:fw-*`, `lg:fw-*` enables the targeting of specific utilities in disparate viewports.