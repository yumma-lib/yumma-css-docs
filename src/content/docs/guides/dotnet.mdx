---
description: Dynamic server-side web apps.
title: Install Yumma CSS with .NET
slug: docs/guides/dotnet
tableOfContents: false
---

export const onCSS = `/* Minified Version */
@import url('/lib/yummacss/dist/yumma.min.css');`;

export const onView = `
<body>
    <div class="ai-c bg-d-lead-4 d-f jc-c h-1/1">
        <h1 class="fs-lg fw-700 tc-white">Yumma CSS + .NET</h1>
    </div>
</body>
`;

<Steps>

    1. **Install Yumma CSS using LibMan:**

        Navigate to the solution your project:

        <Code code="cd project-name\solution-name" lang="bash" title="Terminal" />

        Install the Yumma CSS Library using [LibMan](https://learn.microsoft.com/en-us/aspnet/core/client-side/libman/libman-cli?view=aspnetcore-8.0).

        <Code code="libman install yummacss@latest --provider jsdelivr --destination lib/yummacss" lang="bash" title="Installing dependencies..." />

        <Aside type="note" title="Install without LibMan">
            You can also add `yummacss` to your project by right-clicking your project in **Solution Explorer** and selecting **Add** > **Client-Side Library**.
        </Aside>

    3. **Create a new page in your web app directory.**

        Once you've installed it, you should see the `yummacss` folder inside your `/lib` folder.

        <FileTree>
            - Properties/
            - **wwwroot**
                - css
                    - site.css
                - js
                    - site.js
                - lib/
                    - **yummacss/** generates this
                - favicon.ico
            - Controllers
                - HomeController.cs
            - Models
                - ErrorViewModel.cs
            - Views
                - Home/
                - Shares/
            - appsettings.json
            - libman.json
            - Program.cs
        </FileTree>

    2. **Add** `yummacss` **in your main CSS file:**

        <Code code={onCSS} lang="css" title="css/site.css" />

    3. **Start your .NET development server:**

        You can also press the `F5` key to access the development server.

    4. **Begin using Yumma CSS in your .NET project:**

        You're all set to start using Yumma CSS utility classes in your project.

        <Code code={onView} lang="html" mark={[2,3,4]} title="Shared/_Layout.cshtml" />

</Steps>

---

### Still stuck?

If you're having trouble with Yumma CSS, clone this [ASP.NET (MVC) example](https://github.com/yumma-lib/dotnet-yummacss-example.git).

<Code
  code="git clone https://github.com/yumma-lib/dotnet-yummacss-example.git"
  lang="bash"
  title="Cloning repository..."
/>
