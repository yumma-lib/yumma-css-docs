---
title: Spacing
description: Controls the spacing of child elements.
slug: docs/spacing
---

import { spacingX, spacingY } from "../../../constants/examples/spacing";

Controls the spacing of child elements.

### Spacing X

Controls the left and right X-axis margins of child elements.

<Utility
  classPrefix="s-x-"
  incrementValue={0.25}
  propNames={["margin-left", "margin-right"]}
  range={100}
  unit="rem"
/>

<Live
  data={spacingX}
  useCenter
  useHeight="100px"
  useMarks={["s-x-8"]}
  usePadding
  useVersion="2.1.0"
/>

### Spacing Y

Controls the top and bottom Y-axis margins of child elements.

<Utility
  classPrefix="s-y-"
  incrementValue={0.25}
  propNames={["margin-top", "margin-bottom"]}
  range={100}
  unit="rem"
/>

<Live
  data={spacingY}
  useCenter
  useHeight="300px"
  useMarks={["s-y-8"]}
  usePadding
  useVersion="2.1.0"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:s-x-*`, `md:s-x-*`, `lg:s-x-*`, and `xxl:s-x-*` allows targeting specific utilities in different viewports.

```html "md:s-x-2"
<div class="s-x-1 md:s-x-2 ..."></div>
```

### Using variants

Using `:hover` variants such as `h:s-x-*` allows you to override elements and change their values when hovering over them.

```html "h:s-x-2"
<div class="s-x-1 h:s-x-2 ..."></div>
```
