---
title: Padding
description: Controls the padding of an element.
slug: docs/padding
---

import {
  base,
  paddingBottom,
  paddingLeft,
  paddingRight,
  paddingTop,
  paddingX,
  paddingY,
} from "../../../constants/examples/padding";

Controls the padding of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="p-"
  incrementValue={0.25}
  propNames={["padding"]}
  range={100}
  unit="rem"
/>

<Live
  data={base}
  useCenter
  useHeight="150px"
  useMarks={["p-8"]}
  usePadding
  useVersion="2.1.0"
/>

### Padding bottom

Controls the bottom padding of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="pb-"
  incrementValue={0.25}
  propNames={["padding-bottom"]}
  range={100}
  unit="rem"
/>

<Live
  data={paddingBottom}
  useCenter
  useHeight="150px"
  useMarks={["pb-8"]}
  usePadding
  useVersion="2.1.0"
/>

### Padding left

Controls the left padding of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="pl-"
  incrementValue={0.25}
  propNames={["padding-left"]}
  range={100}
  unit="rem"
/>

<Live
  data={paddingLeft}
  useCenter
  useHeight="150px"
  useMarks={["pl-8"]}
  usePadding
  useVersion="2.1.0"
/>

### Padding right

Controls the right padding of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="pr-"
  incrementValue={0.25}
  propNames={["padding-right"]}
  range={100}
  unit="rem"
/>

<Live
  data={paddingRight}
  useCenter
  useHeight="150px"
  useMarks={["pr-8"]}
  usePadding
  useVersion="2.1.0"
/>

### Padding top

Controls the top padding of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="pt-"
  incrementValue={0.25}
  propNames={["padding-top"]}
  range={100}
  unit="rem"
/>

<Live
  data={paddingTop}
  useCenter
  useHeight="150px"
  useMarks={["pt-8"]}
  usePadding
  useVersion="2.1.0"
/>

### Padding X

Controls the left and right paddings of an element at once.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="px-"
  incrementValue={0.25}
  propNames={["padding-left", "padding-right"]}
  range={100}
  unit="rem"
/>

<Live
  data={paddingX}
  useCenter
  useHeight="150px"
  useMarks={["px-8"]}
  usePadding
  useVersion="2.1.0"
/>

### Padding Y

Controls the top and bottom paddings of an element at once.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="py-"
  incrementValue={0.25}
  propNames={["padding-top", "padding-bottom"]}
  range={100}
  unit="rem"
/>

<Live
  data={paddingY}
  useCenter
  useHeight="150px"
  useMarks={["py-8"]}
  usePadding
  useVersion="2.1.0"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:p-*`, `md:p-*`, `lg:p-*`, and `xxl:p-*` allows targeting specific utilities in different viewports.

```html "md:p-2"
<div class="p-1 md:p-2 ..."></div>
```

### Using variants

Using `:hover` variants such as `h:p-*` allows you to override elements and change their values when hovering over them.

```html "h:p-2"
<div class="p-1 h:p-2 ..."></div>
```
