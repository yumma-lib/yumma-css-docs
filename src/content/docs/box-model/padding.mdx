---
title: Padding
description: Controls the padding of an element.
slug: docs/padding
---

Controls the padding of an element.

<Utility 
  classPrefix='p-' 
  classValues={['padding']} 
  range={100} 
  increment={0.25} 
  unit='rem' 
  additionalClasses={[
    { name: 'auto', value: 'auto' }
  ]} 
/>

## Padding Bottom

Controls the bottom padding of an element.

<Utility 
  classPrefix='pb-' 
  classValues={['padding-bottom']} 
  range={100} 
  increment={0.25} 
  unit='rem' 
  additionalClasses={[
    { name: 'auto', value: 'auto' }
  ]} 
/>

## Padding Left

Controls the left padding of an element.

<Utility 
  classPrefix='pl-' 
  classValues={['padding-left']} 
  range={100} 
  increment={0.25} 
  unit='rem' 
  additionalClasses={[
    { name: 'auto', value: 'auto' }
  ]} 
/>

## Padding Right

Controls the right padding of an element.

<Utility 
  classPrefix='pr-' 
  classValues={['padding-right']} 
  range={100} 
  increment={0.25} 
  unit='rem' 
  additionalClasses={[
    { name: 'auto', value: 'auto' }
  ]} 
/>

## Padding Top

Controls the top padding of an element.

<Utility 
  classPrefix='pt-' 
  classValues={['padding-top']} 
  range={100} 
  increment={0.25} 
  unit='rem' 
  additionalClasses={[
    { name: 'auto', value: 'auto' }
  ]} 
/>

## Padding X

Controls the left and right paddings of an element at once.

<Utility 
  classPrefix='px-' 
  classValues={['padding-left', 'padding-right']}
  range={100} 
  increment={0.25} 
  unit='rem'
/>

## Padding Y

Controls the top and bottom paddings of an element at once.

<Utility 
  classPrefix='py-' 
  classValues={['padding-top', 'padding-bottom']} 
  range={100} 
  increment={0.25} 
  unit='rem'
/>

### Using breakpoints

The utilization of breakpoint modifiers like `sm:p-*`, `md:p-*`, `lg:p-*` enables the targeting of specific utilities in disparate viewports.