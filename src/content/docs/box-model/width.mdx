---
title: Width
description: Controls the width of an element.
slug: docs/width
---

import { base, widthPercentages } from "../../../constants/examples/width";

Controls the width of an element.

<Utility
  additionalClasses={[
    { name: "1/1", value: "100dvh" },
    { name: "1/2", value: "50dvh" },
    { name: "auto", value: "auto" },
    { name: "fc", value: "fit-content" },
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
    { name: "max", value: "max-content" },
    { name: "min", value: "min-content" },
  ]}
  classPrefix="w-"
  incrementValue={0.25}
  propNames={["width"]}
  range={100}
  unit="rem"
/>

<Live
  data={base}
  useCenter
  useHeight="300px"
  useMarks={["w-12", "w-20", "w-28", "w-36"]}
  usePadding
  useVersion="2.1.0"
/>

### Using percentages

Percentage utilities let you change an element's height based on its container size.

<Live
  data={widthPercentages}
  useCenter
  useHeight="200px"
  useMarks={["w-full", "w-half"]}
  usePadding
  useVersion="2.1.0"
/>

### Max width

Controls the maximum width of an element.

<Utility
  additionalClasses={[
    { name: "1/1", value: "100dvh" },
    { name: "1/2", value: "50dvh" },
    { name: "auto", value: "auto" },
    { name: "fc", value: "fit-content" },
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
    { name: "max", value: "max-content" },
    { name: "min", value: "min-content" },
  ]}
  classPrefix="max-w-"
  incrementValue={0.25}
  propNames={["max-width"]}
  range={100}
  unit="rem"
/>

### Min width

Controls the minimum width of an element.

<Utility
  additionalClasses={[
    { name: "1/1", value: "100dvh" },
    { name: "1/2", value: "50dvh" },
    { name: "auto", value: "auto" },
    { name: "fc", value: "fit-content" },
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
    { name: "max", value: "max-content" },
    { name: "min", value: "min-content" },
  ]}
  classPrefix="min-w-"
  incrementValue={0.25}
  propNames={["min-width"]}
  range={100}
  unit="rem"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:w-*`, `md:w-*`, `lg:w-*`, and `xxl:w-*` allows targeting specific utilities in different viewports.

```html "md:w-2"
<div class="w-1 md:w-2 ..."></div>
```

### Using variants

Using `:hover` variants such as `h:w-*` allows you to override elements and change their values when hovering over them.

```html "h:w-2"
<div class="w-1 h:w-2 ..."></div>
```
