---
title: Margin
description: Controls the margin of an element.
slug: docs/margin
---

import {
  base,
  marginBottom,
  marginLeft,
  marginRight,
  marginTop,
  marginX,
  marginY,
} from "../../../constants/examples/margin";

Controls the margin of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="m-"
  incrementValue={0.25}
  propNames={["margin"]}
  range={100}
  unit="rem"
/>

<Live
  data={base}
  useCenter
  useHeight="200px"
  useMarks={["m-8"]}
  usePadding
  useVersion="2.1.0"
/>

### Margin bottom

Controls the bottom margin of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="mb-"
  incrementValue={0.25}
  propNames={["margin-bottom"]}
  range={100}
  unit="rem"
/>

<Live
  data={marginBottom}
  useCenter
  useHeight="200px"
  useMarks={["mb-4"]}
  usePadding
  useVersion="2.1.0"
/>

### Margin left

Controls the left margin of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="ml-"
  incrementValue={0.25}
  propNames={["margin-left"]}
  range={100}
  unit="rem"
/>

<Live
  data={marginLeft}
  useCenter
  useHeight="200px"
  useMarks={["mb-4"]}
  usePadding
  useVersion="2.1.0"
/>

### Margin right

Controls the right margin of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="mr-"
  incrementValue={0.25}
  propNames={["margin-right"]}
  range={100}
  unit="rem"
/>

<Live
  data={marginRight}
  useCenter
  useHeight="200px"
  useMarks={["mb-4"]}
  usePadding
  useVersion="2.1.0"
/>

### Margin top

Controls the top margin of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="mt-"
  incrementValue={0.25}
  propNames={["margin-top"]}
  range={100}
  unit="rem"
/>

<Live
  data={marginTop}
  useCenter
  useHeight="200px"
  useMarks={["mb-4"]}
  usePadding
  useVersion="2.1.0"
/>

### Margin X

Controls the left and right margins of an element at once.

<Utility
  classPrefix="mx-"
  incrementValue={0.25}
  propNames={["margin-left", "margin-right"]}
  range={100}
  unit="rem"
/>

<Live
  data={marginX}
  useCenter
  useHeight="200px"
  useMarks={["mb-4"]}
  usePadding
  useVersion="2.1.0"
/>

### Margin Y

Controls the top and bottom margins of an element at once.

<Utility
  classPrefix="my-"
  incrementValue={0.25}
  propNames={["margin-top", "margin-bottom"]}
  range={100}
  unit="rem"
/>

<Live
  data={marginY}
  useCenter
  useHeight="200px"
  useMarks={["mb-4"]}
  usePadding
  useVersion="2.1.0"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:m-*`, `md:m-*`, `lg:m-*`, and `xxl:m-*` allows targeting specific utilities in different viewports.

```html "md:m-2"
<div class="m-1 md:m-2 ..."></div>
```

### Using variants

Using `:hover` variants such as `h:m-*` allows you to override elements and change their values when hovering over them.

```html "h:m-2"
<div class="m-1 h:m-2 ..."></div>
```
