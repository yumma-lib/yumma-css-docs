---
title: Margin
description: Controls the margin of an element.
slug: docs/margin
---

Controls the margin of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="m-"
  classValues={["margin"]}
  increment={0.25}
  range={100}
  unit="rem"
/>

### Margin Bottom

Controls the bottom margin of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="mb-"
  classValues={["margin-bottom"]}
  increment={0.25}
  range={100}
  unit="rem"
/>

### Margin Left

Controls the left margin of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="ml-"
  classValues={["margin-left"]}
  increment={0.25}
  range={100}
  unit="rem"
/>

### Margin Right

Controls the right margin of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="mr-"
  classValues={["margin-right"]}
  increment={0.25}
  range={100}
  unit="rem"
/>

### Margin Top

Controls the top margin of an element.

<Utility
  additionalClasses={[{ name: "auto", value: "auto" }]}
  classPrefix="mt-"
  classValues={["margin-top"]}
  increment={0.25}
  range={100}
  unit="rem"
/>

### Margin X

Controls the left and right margins of an element at once.

<Utility
  classPrefix="mx-"
  classValues={["margin-left", "margin-right"]}
  increment={0.25}
  range={100}
  unit="rem"
/>

### Margin Y

Controls the top and bottom margins of an element at once.

<Utility
  classPrefix="my-"
  classValues={["margin-top", "margin-bottom"]}
  increment={0.25}
  range={100}
  unit="rem"
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:m-*`, `md:m-*`, `lg:m-*`, and `xxl:m-*` allows targeting specific utilities in different viewports.

```html "md:m-2"
<div class="m-1 md:m-2 ..."></div>
```

### Using variants

Using `:hover` variants such as `h:m-*` allows you to override elements and change their values when hovering over them.

```html "h:m-2"
<div class="m-1 h:m-2 ..."></div>
```
