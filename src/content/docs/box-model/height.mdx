---
title: Height
description: Controls the height of an element.
slug: docs/height
---

import { base } from "../../../constants/examples/height";

Controls the height of an element.

<Utility
  classPrefix="h-"
  classValues={["height"]}
  range={100}
  increment={0.25}
  unit="rem"
  additionalClasses={[
    { name: "1/1", value: "100dvh" },
    { name: "1/2", value: "50dvh" },
    { name: "auto", value: "auto" },
    { name: "fc", value: "fit-content" },
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
    { name: "max", value: "max-content" },
    { name: "min", value: "min-content" },
  ]}
/>

<Live
  data={base}
  useCenter
  useHeight="200px"
  useMarks={["h-10", "h-20", "h-30", "h-40"]}
  usePadding
  useVersion="release"
/>

### Max Height

Controls the maximum height of an element.

<Utility
  classPrefix="max-h-"
  classValues={["max-height"]}
  range={100}
  increment={0.25}
  unit="rem"
  additionalClasses={[
    { name: "1/1", value: "100dvh" },
    { name: "1/2", value: "50dvh" },
    { name: "auto", value: "auto" },
    { name: "fc", value: "fit-content" },
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
    { name: "max", value: "max-content" },
    { name: "min", value: "min-content" },
  ]}
/>

### Min Height

Controls the minimum height of an element.

<Utility
  classPrefix="min-h-"
  classValues={["min-height"]}
  range={100}
  increment={0.25}
  unit="rem"
  additionalClasses={[
    { name: "1/1", value: "100dvh" },
    { name: "1/2", value: "50dvh" },
    { name: "auto", value: "auto" },
    { name: "fc", value: "fit-content" },
    { name: "full", value: "100%" },
    { name: "half", value: "50%" },
    { name: "max", value: "max-content" },
    { name: "min", value: "min-content" },
  ]}
/>

---

### Using breakpoints

Using responsive breakpoints like `sm:h-*`, `md:h-*`, `lg:h-*`, and `xxl:h-*` allows targeting specific utilities in different viewports.

```html "md:h-2"
<div class="h-1 md:h-2 ..."></div>
```

### Using variants

Using `:hover` variants such as `h:h-*` allows you to override elements and change their values when hovering over them.

```html "h:h-2"
<div class="h-1 h:h-2 ..."></div>
```
