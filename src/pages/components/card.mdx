---
description: Add these Yumma CSS Card components to organize and display content.
title: Yumma CSS Card Section
hide_table_of_contents: true
---

import Preview from '@site/src/components/preview/preview';
import { basic, withImage, centralized, centralizedWithIcon } from '../../components/Sample/card';

# Yumma CSS Card

Add these Yumma CSS Card components to organize and display content.

### Basic

<Preview
  data = {basic}
  useCenter = { true }
  useHeight = '230px' 
  usePadding
  useTabs
  useVersion = 'release'
/>

## With image

<Preview
  data = {withImage}
  useCenter = { true }
  useHeight = '420px' 
  usePadding
  useTabs
  useVersion = 'release'
/>

### Centralized

<Preview
  data = {centralized}
  useHeight = '270px' 
  usePadding
  useTabs
  useVersion = 'release'
/>

### Centralized with icon

<Preview
  data = {centralizedWithIcon}
  useHeight = '270px' 
  usePadding
  useTabs
  useVersion = 'release'
/>