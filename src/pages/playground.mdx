---
title: Playground
---

import { Sandpack } from "@codesandbox/sandpack-react";
import { freeCodeCampDark } from "@codesandbox/sandpack-themes";

# Playground

Welcome to the playground page! Here you can play with Yumma CSS, try different styles, layouts and elements and see how they interact in real time using React.

<Sandpack
 theme={freeCodeCampDark}
 files={{
"/App.js": {
code: `import React from "react";

import Layout from "./Layout";
import Player from "./Player";

function App() {
   return (
      <div>
        <Layout /> 
        <Player />
      </div>
   );
}

export default App;
`,
},

"/Layout.js": {
code: `import React from "react";

import { IoMusicalNotes } from "react-icons/io5";

import "yummacss/dist/yumma.css";

function Layout() {
   return (
      <div>
         <div className="h-1/1 bg-d-lead-6">
            <div className="ins h-1/1">
               <IoMusicalNotes color={'#fff'} size={100} />
            </div>
         </div>
      </div>
   );
}

export default Layout;
`,
},

"/MediaItem.js": {
code: `import React from "react";

import "yummacss/dist/yumma.css";

function MediaItem() {
   return (
      <div>
         <div className="d-f ai-c cg-3 c-p w-full rad-1">
            <div className="pos-r min-h-3 min-w-3 ovf-h ml-1">
               <img className="dim-10" src={'https://f4.bcbits.com/img/a3135145324_16.jpg'} alt="Media Item" style={{ aspectRatio: '1 / 1'}} />
            </div>
            <div className="d-f fd-c rg-1 ovf-h">
               <p className="t-white">My Old Me</p>
               <p className="t-l-lead-4 fs-sm">The Wiolets</p>
            </div>
         </div>
      </div>
   );
}

export default MediaItem;
`,
},

"/Player.js": {
code: `import React from "react";

import PlayerContent from "./PlayerContent";

import "yummacss/dist/yumma.css";

function Player() {
   return (
      <div className="pos-f dir-b bg-black w-full h-10 py-1">
         <PlayerContent />
      </div>
   );
}

export default Player;
`,
},

"/PlayerContent.js": {
code: `import React, { useState } from "react";
import { IoPlay, IoPause } from "react-icons/io5";

import MediaItem from "./MediaItem";

import "yummacss/dist/yumma.css";

function PlayerContent() {
   const [isPlaying, setIsPlaying] = useState(false);

   const togglePlayPause = () => {
      setIsPlaying(!isPlaying);
   };

   return (
      <div>
         <div className="d-g gtc-2 h-full">
            <div className="d-f w-full jc-s">
               <div className="d-f ai-c gc-4">
                 <MediaItem />
               </div>
            </div>
            <div className="d-f w-full jc-e ai-c" style={{ gridColumn: 'auto' }}>
               <div className="dim-4 ins rad-8 bg-white p-1 c-p mr-1">
                 {isPlaying ? (
                     <IoPause size={30} className="t-black" onClick={togglePlayPause} />
                 ) : (
                     <IoPlay size={30} className="t-black" onClick={togglePlayPause} />
                 )}
               </div>
            </div>
         </div>
      </div>
   );
}

export default PlayerContent;
`,
},

 }}
 customSetup={{
    dependencies: {
      "yummacss": "latest",
      "react-icons": "latest"
    }
 }}
 template="react"
 options={{
    showNavigator: true,
    showTabs: true,
    editorHeight: 600,
 }}
/>
