---
title: Hover State
---

import ClassPreview from "@site/src/components/ClassPreview/ClassPreview";

# Hover State

Manages the appearance of elements when users hover over them, adding interactivity.

## Background Color

<ClassPreview
  codeData={`
    <div class="bg-gray dim-6 h:bg-black p-1 rad-2"></div>
  `}
  isPadded = {true}
  isCentered = {true}
  isVersion = 'latest'
/>

```html
<div class="bg-gray dim-6 h:bg-black p-1 rad-2"></div>
```

## Border Color

<ClassPreview
  codeData={`
    <div class="bc-l-gray-6 bg-white h-6 h:bc-black p-1 rad-1 w-6"></div>
  `}
  isPadded = {true}
  isCentered = {true}
  isVersion = 'latest'
/>

```html
<div class="bc-l-gray-6 bg-white h-6 h:bc-black p-1 rad-1 w-6"></div>
```

## Text Color

<ClassPreview
  codeData={`
    <h1 class="h:t-black rad-1 t-gray">Hover me</h1>
  `}
  isPadded = {true}
  isCentered = {true}
  isVersion = 'latest'
/>

```html
<h1 class="h:t-black rad-1 t-gray">Hover me</h1>
```