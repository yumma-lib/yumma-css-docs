---
title: Padding
description: Controls the padding of an element.
slug: /padding
---

import Preview from '@site/src/components/Preview/Preview';
import Utility from '@site/src/components/Utility/Utility';

Controls the padding of an element.

<Utility 
  classPrefix='p-' 
  classValue={['padding']} 
  range={100} 
  increment={0.75} 
  unit='rem' 
  additionalClasses={[
    { name: 'auto', value: 'auto' }
  ]}
/>

## Padding Bottom

Controls the bottom padding of an element.

<Utility 
  classPrefix='pb-' 
  classValue={['padding-bottom']} 
  range={100} 
  increment={0.75} 
  unit='rem' 
  additionalClasses={[
    { name: 'auto', value: 'auto' }
  ]} 
/>

## Padding Left

Controls the left padding of an element.

<Utility 
  classPrefix='pl-' 
  classValue={['padding-left']} 
  range={100} 
  increment={0.75} 
  unit='rem' 
  additionalClasses={[
    { name: 'auto', value: 'auto' }
  ]} 
/>

## Padding Right

Controls the right padding of an element.

<Utility 
  classPrefix='pr-' 
  classValue={['padding-right']} 
  range={100} 
  increment={0.75} 
  unit='rem' 
  additionalClasses={[
    { name: 'auto', value: 'auto' }
  ]} 
/>

## Padding Top

Controls the top padding of an element.

<Utility 
  classPrefix='pt-' 
  classValue={['padding-top']} 
  range={100} 
  increment={0.75} 
  unit='rem' 
  additionalClasses={[
    { name: 'auto', value: 'auto' }
  ]} 
/>

## Padding X

Controls the left and right paddings of an element at once.

<Utility 
  classPrefix='px-' 
  classValue={['padding-left', 'padding-right']}
  range={100} 
  increment={0.75} 
  unit='rem'
/>

## Padding Y

Controls the top and bottom paddings of an element at once.

<Utility 
  classPrefix='py-' 
  classValue={['padding-top', 'padding-bottom']} 
  range={100} 
  increment={0.75} 
  unit='rem'
/>