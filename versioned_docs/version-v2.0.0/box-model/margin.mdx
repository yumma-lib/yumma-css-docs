---
title: Margin
description: Controls the margin of an element.
slug: /margin
---

import YmaUtility from '@site/src/components/YmaUtility/YmaUtility';
import YmaPreview from '@site/src/components/YmaPreview/YmaPreview';

Controls the margin of an element.

<YmaUtility 
  baseClassName='m-' 
  propertyNames={['margin']}
  range={100} 
  increment={0.25} 
  unit='rem' 
  additionalClasses={[
    {name: 'auto', value: 'auto'}
  ]} 
/>

## Margin Bottom

Controls the bottom margin of an element.

<YmaUtility 
  baseClassName='mb-' 
  propertyNames={['margin-bottom']} 
  range={100} 
  increment={0.25} 
  unit='rem' 
  additionalClasses={[
    {name: 'auto', value: 'auto'}
  ]} 
/>

## Margin Left

Controls the left margin of an element.

<YmaUtility 
  baseClassName='ml-' 
  propertyNames={['margin-left']} 
  range={100} 
  increment={0.25} 
  unit='rem' 
  additionalClasses={[
    {name: 'auto', value: 'auto'}
  ]} 
/>

## Margin Right

Controls the right margin of an element.

<YmaUtility 
  baseClassName='mr-' 
  propertyNames={['margin-right']} 
  range={100} 
  increment={0.25} 
  unit='rem' 
  additionalClasses={[
    {name: 'auto', value: 'auto'}
  ]} 
/>

## Margin Top

Controls the top margin of an element.

<YmaUtility 
  baseClassName='mt-' 
  propertyNames={['margin-top']} 
  range={100} 
  increment={0.25} 
  unit='rem' 
  additionalClasses={[
    {name: 'auto', value: 'auto'}
  ]} 
/>

## Margin X

Controls the left and right margins of an element at once.

<YmaUtility 
  baseClassName='mx-' 
  propertyNames={['margin-left', 'margin-right']}
  range={100} 
  increment={0.25} 
  unit='rem'
/>

## Margin Y

Controls the top and bottom margins of an element at once.

<YmaUtility 
  baseClassName='my-' 
  propertyNames={['margin-top', 'margin-bottom']} 
  range={100} 
  increment={0.25} 
  unit='rem'
/>

### Using breakpoints

The utilization of breakpoint modifiers like `sm:m-*`, `md:m-*`, `lg:m-*` enables the targeting of specific utilities in disparate viewports.