---
title: Grid Auto Flow
description: Controls the automatic placement of grid elements.
slug: /grid-auto-flow
---

import Preview from '@site/src/components/preview/preview';
import Utility from '@site/src/components/utility/utility';

Controls the automatic placement of grid elements.

<Utility
  data={[
    {
        className: 'gaf-r',
        properties: [
            'grid-auto-flow: row;'
        ]
    },
    {
        className: 'gaf-c',
        properties: [
            'grid-auto-flow: column;'
        ]
    },
    {
        className: 'gaf-d',
        properties: [
            'grid-auto-flow: dense;'
        ]
    }
  ]}
/>

### Using breakpoints

The utilization of breakpoint modifiers like `sm:gaf-*`, `md:gaf-*`, `lg:gaf-*` enables the targeting of specific utilities in disparate viewports.