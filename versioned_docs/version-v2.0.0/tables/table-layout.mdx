---
title: Table Layout
description: Controls the table cell layout algorithm.
slug: /table layout
---

import Utility from '@site/src/components/Utility/Utility';

Controls the table cell layout algorithm.

<Utility
  data={[
    {
      className: 'tl-auto',
      properties: [
        'table-layout: auto;'
      ]
    },
    {
      className: 'tl-f',
      properties: [
        'table-layout: fixed;'
      ]
    }
  ]}
/>

### Using breakpoints

The utilization of breakpoint modifiers like `sm:tl-*`, `md:tl-*`, `lg:tl-*` enables the targeting of specific utilities in disparate viewports.