---
title: Pointer Events
description: Controls how an item responds to pointer events.
slug: /pointer-events
---

import Preview from '@site/src/components/preview/preview';
import Utility from '@site/src/components/utility/utility';

Controls how an item responds to pointer events.

<Utility
  data={[
    {
        className: 'pe-auto',
        properties: [
            'pointer-events: auto;'
        ]
    },
    {
        className: 'pe-none',
        properties: [
            'pointer-events: none;'
        ]
    }
  ]}
/>

### Using breakpoints

The utilization of breakpoint modifiers like `sm:pe-*`, `md:pe-*`, `lg:pe-*` enables the targeting of specific utilities in disparate viewports.